// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

/// @title Milestone Escrow (Demo / Placeholder)
/// @notice Archivo de ejemplo para que el enlace del README no falle en el chequeo de links.
/// @dev Reemplazar por la implementación real cuando corresponda.
contract MilestoneEscrow_OZDemo {
    // Placeholder: sin lógica. Agregue la implementación real más adelante.
    address public owner;
    constructor() { owner = msg.sender; }
}
